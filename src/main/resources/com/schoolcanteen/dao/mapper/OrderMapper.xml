<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.schoolcanteen.dao.mapper.OrderMapper">

    <resultMap id="OrderResultMap" type="com.schoolcanteen.entity.Order">
        <id property="orderid" column="orderid" />
        <result property="orderNo" column="order_no" />
        <result property="studentId" column="student_id" />
        <result property="consumeDate" column="consume_date" />
        <result property="mealType" column="meal_type" />
        <result property="dishInfo" column="dish_info" typeHandler="com.schoolcanteen.handler.JsonNodeTypeHandler"/>
        <result property="dishCount" column="dish_count" />
        <result property="totalAmount" column="total_amount" />
        <result property="payStatus" column="pay_status" />
        <result property="orderStatus" column="order_status" />
        <result property="createTime" column="create_time" />
        <result property="payTime" column="pay_time" />
        <result property="completeTime" column="complete_time" />
    </resultMap>

    <select id="selectByOrderid" resultMap="OrderResultMap">
        SELECT * FROM t_order WHERE order_id = #{orderid}
    </select>

    <select id="selectByOrderNo" resultMap="OrderResultMap">
        SELECT * FROM t_order WHERE order_no = #{orderNo}
    </select>

    <select id="selectByStudentId" resultMap="OrderResultMap">
        SELECT * FROM t_order
        WHERE student_id = #{studentId}
        <if test="startDate != null">
            AND DATE(create_time) >= DATE(#{startDate})
        </if>
        <if test="endDate != null">
            AND DATE(create_time) &lt;= DATE(#{endDate})
        </if>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.schoolcanteen.entity.Order" useGeneratedKeys="true" keyProperty="orderid">
        INSERT INTO t_order (order_no, student_id, consume_date, meal_type, dish_info, dish_count, total_amount, pay_status, order_status, create_time, pay_time, complete_time)
        VALUES (#{orderNo}, #{studentId}, #{consumeDate}, #{mealType}, #{dishInfo, typeHandler=com.schoolcanteen.handler.JsonNodeTypeHandler}, #{dishCount}, #{totalAmount}, #{payStatus}, #{orderStatus}, #{createTime}, #{payTime}, #{completeTime})
    </insert>

    <update id="updateStatus">
        UPDATE t_order
        SET pay_status = #{payStatus},
            order_status = #{orderStatus}
        WHERE order_id = #{orderid}
    </update>

    <select id="sumTotalAmountByStudent" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(total_amount), 0)
        FROM t_order
        WHERE student_id = #{studentId}
        <if test="startDate != null">
            AND DATE(create_time) >= DATE(#{startDate})
        </if>
        <if test="endDate != null">
            AND DATE(create_time) &lt;= DATE(#{endDate})
        </if>
    </select>
</mapper>